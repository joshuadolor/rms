# --- Reverse proxy (recommended for dev: no CORS) ---
# When set, Vite proxies /api and /sanctum to this URL. Leave VITE_API_URL empty so the app uses same-origin /api.
# Docker Compose sets this to http://api:3000; for local dev (API on same machine) use http://localhost:3000.
# VITE_PROXY_TARGET=http://localhost:3000

# Base URL for the API (used by services/api.js). No trailing slash; must end with /api.
# With proxy: leave empty so requests go to same origin and get proxied.
# Without proxy: set to full API URL (e.g. http://localhost:3000/api).
# VITE_API_URL=

# OAuth: after provider sign-in, backend should redirect to this URL with ?token=... (&user=... optional).
# Default used by auth.service is: frontend origin + /login/callback (e.g. http://localhost:8080/login/callback)
