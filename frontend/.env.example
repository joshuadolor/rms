# --- Reverse proxy (recommended for dev: no CORS) ---
# When set, Vite proxies /api and /sanctum to this URL. Leave VITE_API_URL empty so the app uses same-origin /api.
# VITE_PROXY_TARGET=http://localhost:3000

# Base URL for the API. No trailing slash; must end with /api. With proxy, leave empty.
# VITE_API_URL=

# --- SSO (Google, Facebook, Instagram) ---
# Google: Sign-In uses this. Create OAuth 2.0 client (Web) in Google Cloud Console; add authorized JS origins and redirect URIs.
# VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Facebook: redirect_uri will be {origin}/login/callback?provider=facebook. Add that exact URL in Facebook App > Facebook Login settings.
# VITE_FACEBOOK_APP_ID=your-facebook-app-id

# Instagram: redirect_uri will be {origin}/login/callback?provider=instagram. Use Instagram Basic Display or Graph API app.
# VITE_INSTAGRAM_APP_ID=your-instagram-app-id
